import { makeStyles } from "@material-ui/core";

const useTripTransportationStyle = makeStyles((theme) => ({
  selectField: {
    width: "100%",
  },
  selectIcon: {
    marginRight: theme.spacing(1),
  },
}));

export default useTripTransportationStyle;
